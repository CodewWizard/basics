#include<stdio.h>

void sort(int a1[], int a2[], int n1, int n2){
    int gap = 0, first = -1, last;
    while (last )
    {
        gap = ( n1 + n2 ) / 2;
        first++;
        last = gap;
        if(first)
     }
     
}

int main(){
    int a1[] = {1, 4, 7, 8, 10};
    int a2[] = {2, 3, 9};
    int n1 = 5;
    int n2 = 3;
    sort(a1, a2, n1, n2);
}